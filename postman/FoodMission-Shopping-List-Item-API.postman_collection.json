{
	"info": {
		"_postman_id": "shopping-list-item-api-collection",
		"name": "FoodMission Shopping List Item API",
		"description": "Collection for FoodMission Shopping List Item API endpoints.\n\n**IMPORTANT:** Make sure the `base_url` variable is set to `http://localhost:3000` (or your backend server URL) in your Postman environment. Do NOT use a frontend URL (like localhost:5173 or localhost:4200).\n\nThe API uses the global prefix `/api/v1`, so all endpoints are under `/api/v1/...`\n\n**Note:** The `toggle-checked` endpoint requires a `pantryId` query parameter. When checking an item (checked=true), it will create a pantry item if the user's `shouldAutoAddToPantry` preference is enabled.",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
	},
	"item": [
		{
			"name": "Create Shopping List Item",
			"request": {
				"method": "POST",
				"header": [
					{
						"key": "Content-Type",
						"value": "application/json"
					},
					{
						"key": "Authorization",
						"value": "Bearer {{jwt_token}}",
						"type": "text"
					}
				],
				"body": {
					"mode": "raw",
					"raw": "{\n  \"shoppingListId\": \"{{shopping_list_id}}\",\n  \"foodId\": \"{{food_id}}\",\n  \"quantity\": 2,\n  \"unit\": \"KG\",\n  \"notes\": \"Buy organic if available\",\n  \"checked\": false\n}",
					"options": {
						"raw": {
							"language": "json"
						}
					}
				},
				"url": {
					"raw": "{{base_url}}/api/v1/shopping-list-items",
					"host": [
						"{{base_url}}"
					],
					"path": [
						"api",
						"v1",
						"shopping-list-items"
					]
				},
				"description": "Creates a new Shopping List Item and adds it to the specified shopping list. The shoppingListId must belong to the authenticated user. Requires user or admin role.\n\n**Rate Limit:** 10 requests per minute\n\n**Required fields:**\n- `shoppingListId` (UUID): The ID of the shopping list to add the item to\n- `foodId` (UUID): The ID of the food item to add\n- `quantity` (number, min 1): The quantity of the item\n- `unit` (enum): The unit of measurement (PIECES, G, KG, ML, L, CUPS). Defaults to PIECES if not provided.\n- `checked` (boolean): Whether the item is checked off. Defaults to false.\n\n**Optional fields:**\n- `notes` (string, max 500 chars): Additional notes for the item"
			},
			"response": [],
			"event": [
				{
					"listen": "prerequest",
					"script": {
						"exec": [
							"// Set default food_id if not exists",
							"const defaultFoodId = '00d2eca5-0377-448b-b74e-d5ae25e9c944';",
							"if (!pm.environment.get('food_id') || pm.environment.get('food_id') === '') {",
							"    pm.environment.set('food_id', defaultFoodId);",
							"}",
							"// Warn if shopping_list_id is not set",
							"if (!pm.environment.get('shopping_list_id') || pm.environment.get('shopping_list_id') === '') {",
							"    console.warn('Warning: shopping_list_id is not set. Please set it manually or create a shopping list first.');",
							"}"
						],
						"type": "text/javascript"
					}
				},
				{
					"listen": "test",
					"script": {
						"exec": [
							"if (pm.response.code === 201) {",
							"    const jsonData = pm.response.json();",
							"    pm.environment.set('shopping_list_item_id', jsonData.id);",
							"}"
						],
						"type": "text/javascript"
					}
				}
			]
		},
		{
			"name": "Toggle Checked Status",
			"request": {
				"method": "PATCH",
				"header": [
					{
						"key": "Authorization",
						"value": "Bearer {{jwt_token}}",
						"type": "text"
					}
				],
				"url": {
					"raw": "{{base_url}}/api/v1/shopping-list-items/{{shopping_list_item_id}}/toggle-checked?pantryId={{pantry_id}}",
					"host": [
						"{{base_url}}"
					],
					"path": [
						"api",
						"v1",
						"shopping-list-items",
						"{{shopping_list_item_id}}",
						"toggle-checked"
					],
					"query": [
						{
							"key": "pantryId",
							"value": "{{pantry_id}}",
							"description": "Target pantry ID where the item will be added when checked (required)",
							"disabled": false
						}
					]
				},
				"description": "Toggle the checked status of a shopping list item. When checking an item (willBeChecked = true), creates a pantry item from the shopping list item if the user's `shouldAutoAddToPantry` preference is enabled. The pantryId query parameter is required. Requires user or admin role.\n\n**Behavior:**\n- If item is unchecked (checked=false) and you toggle it: item becomes checked (checked=true) and a pantry item is created (if user preference allows)\n- If item is checked (checked=true) and you toggle it: item becomes unchecked (checked=false) and no pantry item is created\n\n**Required query parameters:**\n- `pantryId` (UUID): The ID of the pantry where the item will be added when checked. Must belong to the authenticated user.\n\n**Possible responses:**\n- 200: Item checked status toggled successfully\n- 400: Bad Request - pantryId is required or invalid\n- 401: Unauthorized - JWT token required\n- 403: Forbidden - insufficient permissions\n- 404: Shopping list item or pantry not found\n- 409: Conflict - food item already exists in pantry"
			},
			"response": [],
			"event": [
				{
					"listen": "prerequest",
					"script": {
						"exec": [
							"// Warn if shopping_list_item_id is not set",
							"if (!pm.environment.get('shopping_list_item_id') || pm.environment.get('shopping_list_item_id') === '') {",
							"    console.warn('Warning: shopping_list_item_id is not set. Please create a shopping list item first using the Create Shopping List Item endpoint.');",
							"}",
							"// Warn if pantry_id is not set",
							"if (!pm.environment.get('pantry_id') || pm.environment.get('pantry_id') === '') {",
							"    console.warn('Warning: pantry_id is not set. Please set it manually or create a pantry first using the Pantry API collection.');",
							"}"
						],
						"type": "text/javascript"
					}
				}
			]
		}
	],
	"variable": [
		{
			"key": "base_url",
			"value": "http://localhost:3000",
			"type": "string",
			"description": "Backend API server URL. Must be the NestJS backend (default: http://localhost:3000). Do NOT use a frontend URL."
		},
		{
			"key": "jwt_token",
			"value": "",
			"type": "string",
			"description": "JWT access token (set automatically from Auth API collection)"
		},
		{
			"key": "user_id",
			"value": "",
			"type": "string",
			"description": "User ID from the authenticated user profile (set automatically from Auth API collection)"
		},
		{
			"key": "shopping_list_item_id",
			"value": "",
			"type": "string",
			"description": "Shopping List Item ID (automatically set after creating a shopping list item)"
		},
		{
			"key": "food_id",
			"value": "00d2eca5-0377-448b-b74e-d5ae25e9c944",
			"type": "string",
			"description": "Food ID to use when creating shopping list items (default: seeded food ID)"
		},
		{
			"key": "shopping_list_id",
			"value": "",
			"type": "string",
			"description": "Shopping List ID (set manually or from Shopping List API collection)"
		},
		{
			"key": "pantry_id",
			"value": "",
			"type": "string",
			"description": "Pantry ID (set automatically from Pantry API collection or manually)"
		}
	]
}

